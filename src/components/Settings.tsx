let FOV: number = Math.PI / 2;

let pixel_size: number = 6;
let MODE: number = 0;
let MAX_RECURSION: number = 10;
let darkness: boolean = false;

let WIDTH = 800;
let HEIGHT = 600;

export { pixel_size, MODE, MAX_RECURSION, FOV, WIDTH, HEIGHT, darkness };